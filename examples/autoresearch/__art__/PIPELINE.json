{
  "stages": [
    {
      "name": "git-start",
      "prompt": "Create a new branch named autoresearch/<timestamp> (e.g. autoresearch/mar26-1430) from the current HEAD and make an empty baseline commit with message 'autoresearch baseline'.\n\nWhen done, emit [STAGE_COMPLETE].",
      "mounts": {
        "project": "ro",
        "project:.git": "rw"
      },
      "transitions": [
        {
          "marker": "STAGE_COMPLETE",
          "next": "build"
        }
      ]
    },
    {
      "name": "build",
      "prompt": "You are an autonomous ML researcher. Your goal is to minimize val_bpb by modifying train.py.\n\n1. Read /workspace/project/src/train.py for context.\n2. Read /workspace/results/results.tsv (if it exists) to see past experiments.\n3. Choose an experiment idea — change architecture, optimizer, hyperparameters, batch size, model size, etc.\n4. Modify /workspace/project/src/train.py with your changes.\n\nRules:\n- This project runs on GPU. CUDA is available.\n- Only modify src/train.py. Do NOT modify src/prepare.py or any other file.\n- Do NOT install new packages.\n- Do NOT run train.py or any Python script. You are the build stage — code modification only. Testing is handled by a separate stage.\n- Keep changes focused — one idea per iteration.\n- If you see repeated failures in results.tsv, try a different direction.\n\nWhen done, emit [STAGE_COMPLETE].",
      "mounts": {
        "project": "rw",
        "results": "ro"
      },
      "transitions": [
        {
          "marker": "STAGE_COMPLETE",
          "next": "git-save",
          "prompt": "Code modification complete"
        },
        {
          "marker": "STAGE_ERROR",
          "retry": true,
          "prompt": "Recoverable error"
        }
      ]
    },
    {
      "name": "git-save",
      "prompt": "Stage and commit all changes in the project.\n\nRead the git diff to understand what was changed, then write a concise commit message that describes the experiment (e.g. 'increase hidden dim from 256 to 512').\n\nWhen done, emit [STAGE_COMPLETE].",
      "mounts": {
        "project": "ro",
        "project:.git": "rw"
      },
      "transitions": [
        {
          "marker": "STAGE_COMPLETE",
          "next": "test"
        }
      ]
    },
    {
      "name": "test",
      "prompt": "",
      "command": "apt-get update -qq && apt-get install -y -qq curl > /dev/null && curl -LsSf https://astral.sh/uv/install.sh | sh > /dev/null 2>&1 && export PATH=\"$HOME/.local/bin:$PATH\" && mkdir -p /workspace/cache/uv-python /workspace/cache/uv-cache && ln -sfn /workspace/cache/uv-cache /root/.cache/uv && ln -sfn /workspace/cache/uv-python /root/.local/share/uv/python && cd /workspace/project && UV_PROJECT_ENVIRONMENT=/workspace/venv uv run src/prepare.py && UV_PROJECT_ENVIRONMENT=/workspace/venv uv run src/train.py > /workspace/results/run.log 2>&1; grep '^val_bpb:\\|^peak_rss_mb:' /workspace/results/run.log > /workspace/results/metrics.txt 2>/dev/null; echo '[STAGE_COMPLETE]'",
      "image": "nvidia/cuda:12.4.1-devel-ubuntu22.04",
      "gpu": true,
      "mounts": {
        "project": "ro",
        "results": "rw",
        "cache": "rw",
        "venv": "rw"
      },
      "transitions": [
        {
          "marker": "STAGE_COMPLETE",
          "next": "review"
        }
      ]
    },
    {
      "name": "review",
      "prompt": "You are reviewing the results of an ML experiment.\n\n1. Read /workspace/results/metrics.txt for the key metrics (val_bpb, peak_rss_mb).\n2. If metrics.txt is empty, the run crashed. Read the last 50 lines of /workspace/results/run.log to understand why.\n3. Read /workspace/results/results.tsv to compare with previous experiments.\n4. Decide:\n   - If val_bpb improved (lower than best previous): emit [STAGE_KEEP]\n   - If val_bpb is worse: emit [STAGE_RESET]\n   - If the run crashed: emit [STAGE_RESET]\n5. Append a row to /workspace/results/results.tsv:\n   commit\\tval_bpb\\tmemory_gb\\tstatus\\tdescription\n   - Use `git rev-parse --short HEAD` for the commit hash\n   - status: keep, discard, or crash\n\nIMPORTANT: Always emit exactly one marker: [STAGE_KEEP] or [STAGE_RESET].",
      "mounts": {
        "project": "ro",
        "results": "rw"
      },
      "transitions": [
        {
          "marker": "STAGE_KEEP",
          "next": "git-keep",
          "prompt": "Experiment improved val_bpb — keep the commit"
        },
        {
          "marker": "STAGE_RESET",
          "next": "git-reset",
          "prompt": "Experiment did not improve — revert the commit"
        }
      ]
    },
    {
      "name": "git-keep",
      "prompt": "",
      "command": "echo 'Keeping commit' && echo '[STAGE_COMPLETE]'",
      "image": "alpine/git",
      "mounts": {},
      "transitions": [
        {
          "marker": "STAGE_COMPLETE",
          "next": "build"
        }
      ]
    },
    {
      "name": "git-reset",
      "prompt": "",
      "command": "git config --global --add safe.directory /workspace/project && cd /workspace/project && git reset --hard HEAD~1 && echo '[STAGE_COMPLETE]'",
      "image": "alpine/git",
      "mounts": {
        "project": "rw"
      },
      "transitions": [
        {
          "marker": "STAGE_COMPLETE",
          "next": "build"
        }
      ]
    }
  ],
  "entryStage": "git-start"
}
