{
  "entry": "ingest",
  "stages": [
    {
      "name": "ingest",
      "prompt": "Worker lane {{insertId}} — ingest stage (1/3) for id \"{{id}}\" (kind \"{{kind}}\", lane index {{index}}). Return exactly this text and nothing else:\n\n[OK]",
      "mounts": {},
      "transitions": [
        {
          "marker": "OK",
          "next": "process",
          "prompt": "Ingested {{id}} — move to process"
        }
      ]
    },
    {
      "name": "process",
      "prompt": "Worker lane {{insertId}} — process stage (2/3) for id \"{{id}}\" (kind \"{{kind}}\"). Return exactly this text and nothing else:\n\n[OK]",
      "mounts": {},
      "transitions": [
        {
          "marker": "OK",
          "next": "finalize",
          "prompt": "Processed {{id}} — move to finalize"
        }
      ]
    },
    {
      "name": "finalize",
      "prompt": "Worker lane {{insertId}} — finalize stage (3/3). Use the Write tool to create a file at /workspace/output/{{id}}.txt with these exact contents (three lines, no extra text):\n\nid={{id}}\nkind={{kind}}\nindex={{index}}\n\nAfter writing the file, return exactly this text and nothing else:\n\n[DONE]",
      "mounts": {
        "output": "rw"
      },
      "transitions": [
        {
          "marker": "DONE",
          "next": null,
          "prompt": "Done {{id}} — converge on join"
        }
      ]
    }
  ]
}
