{
  "stages": [
    {
      "name": "test-rw",
      "prompt": "",
      "command": "echo 'hello' > /workspace/src/written.txt 2>/dev/null && cat /workspace/src/written.txt | grep -q hello && echo 'WRITE_OK' || echo 'WRITE_FAIL'; echo '[STAGE_COMPLETE]'",
      "image": "node:22-slim",
      "mounts": {
        "src": "rw"
      },
      "transitions": [
        { "marker": "STAGE_COMPLETE", "next": null }
      ]
    }
  ]
}
